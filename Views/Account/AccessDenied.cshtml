@{
    ViewData["Title"] = "Access denied";
}

<div class="auth-wrap text-center">
    <i class="ti ti-lock" style="font-size:40px;color:var(--text-muted)"></i>
    <h1 class="mt-3 mb-2">Access denied</h1>
    <p class="text-muted mb-4">You don't have permission to view this page.</p>
    <a class="btn-ascent" asp-controller="Home" asp-action="Index">Back to home</a>
</div>
