@model List<ascent_app.Models.Region>
@{
    ViewData["Title"] = "Regions";
    var counts = ViewBag.TrailCounts as Dictionary<int, int> ?? new();
}

<partial name="_AdminNav" />

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="mb-0" style="font-size:22px">Regions</h2>
    <a class="btn-ascent" asp-action="Create"><i class="ti ti-plus"></i> New region</a>
</div>

@if (Model.Count == 0)
{
    <div class="asc-panel text-center text-muted">No regions yet.</div>
}
else
{
    <table class="admin-table">
        <thead>
            <tr><th>Name</th><th>Description</th><th>Trails</th><th></th></tr>
        </thead>
        <tbody>
            @foreach (var region in Model)
            {
                var used = counts.GetValueOrDefault(region.Id);
                <tr>
                    <td><strong>@region.Name</strong></td>
                    <td class="text-secondary">@region.Description</td>
                    <td>@used</td>
                    <td class="text-end text-nowrap">
                        <a class="asc-nav-link" asp-action="Edit" asp-route-id="@region.Id"><i class="ti ti-edit"></i> Edit</a>
                        <form asp-action="Delete" asp-route-id="@region.Id" method="post" class="d-inline"
                              onsubmit="return confirm('Delete @region.Name?');">
                            <button type="submit" class="admin-del"><i class="ti ti-trash"></i> Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
